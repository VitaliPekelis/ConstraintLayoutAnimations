<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lpirro.constraintlayoutanimations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity"
                  android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CircuitActivity"/>
        <activity android:name=".CarouselOf3Activity"/>
        <activity android:name=".ImageActivity" />
        <activity
            android:name=".ShoppingActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".CarouselActivity" />



        <activity android:name=".SpeakerActivity" />
        <activity android:name=".NestedActivity" />

        <activity android:name=".ImageJavaActivity"/>

    </application>

</manifest>